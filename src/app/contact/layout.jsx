export const metadata = {
  title: "Contact - MyNextProject",
};

export default function ContactLayout({ children }) {
  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-100">
      {children}
    </div>
  );
}

